"use client";

/**
 * STUB — replaced by the mobile joystick implementation.
 * Contract: render an on-screen joystick on touch devices only, and write the
 * analog vector into the store via `useGame.getState().setJoystick(x, z)`
 * (x: -1..1 strafe, z: -1..1 forward/back; z negative = forward). Render
 * nothing on non-touch devices.
 */
export function MobileControls() {
  return null;
}
