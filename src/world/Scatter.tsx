"use client";

/**
 * Decorative scatter: instanced trees, rocks, flowers, lanterns, fences, grass
 * tufts. SEAM owned by the natural-environment agent. Stub renders nothing.
 * Props must not block the winding path or overlap building footprints.
 */
export function Scatter() {
  return null;
}
